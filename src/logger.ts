/**
 * The logger, using the native `console`.
 * 
 * If you have more complex logging requirements, feel free to swap out `console` with something else in this module.
 * 
 * @module
 */

const logger = console

export { logger }
